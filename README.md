# Ticket Pipeline Demo

A minimal Flask API used as the sample application for a DevSecOps CI/CD
pipeline portfolio project. The app itself is intentionally simple, the
CI/CD pipeline (build, test, containerize, security scan, deploy) is the
actual project.

## Status

Under active development. See commit history for build progress.
